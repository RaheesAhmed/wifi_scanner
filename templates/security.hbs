{{#> layout}}
<div class="security-container">
    <div class="security-header">
        <h3>WiFi Security Analysis</h3>
        <p>Analyze the security of specific WiFi networks</p>
    </div>
    
    <div class="security-form-container">
        <form id="security-form" class="security-form">
            <div class="form-group">
                <label for="ssid-input">SSID (Network Name)</label>
                <input type="text" id="ssid-input" name="ssid" placeholder="Enter SSID" required>
            </div>
            
            <div class="form-group">
                <label for="bssid-input">BSSID (MAC Address)</label>
                <input type="text" id="bssid-input" name="bssid" placeholder="Format: XX:XX:XX:XX:XX:XX" required>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn primary-btn">
                    <i class="fas fa-shield-alt"></i> Analyze Security
                </button>
            </div>
        </form>
    </div>
    
    <div class="security-results hidden" id="security-results">
        <h3>Security Analysis Results</h3>
        
        <div class="results-summary">
            <div class="network-info">
                <p><strong>SSID:</strong> <span id="result-ssid"></span></p>
                <p><strong>BSSID:</strong> <span id="result-bssid"></span></p>
            </div>
            
            <div class="security-status" id="security-status">
                <div class="status-icon secure hidden" id="status-secure">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="status-icon warning hidden" id="status-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <p id="status-text">Analyzing...</p>
            </div>
        </div>
        
        <div class="vulnerabilities-container">
            <h4>Detected Vulnerabilities</h4>
            <ul id="vulnerabilities-list" class="vulnerabilities-list">
                <!-- Vulnerabilities will be added here dynamically -->
            </ul>
        </div>
        
        <div class="security-recommendations">
            <h4>Recommendations</h4>
            <ul id="recommendations-list" class="recommendations-list">
                <!-- Recommendations will be added here dynamically -->
            </ul>
        </div>
    </div>
    
    <div class="security-info-section">
        <h3>Common WiFi Security Issues</h3>
        
        <div class="security-info-grid">
            <div class="info-card">
                <h4>WEP Encryption</h4>
                <p>WEP is an outdated encryption protocol that can be easily cracked within minutes.</p>
            </div>
            
            <div class="info-card">
                <h4>Open Networks</h4>
                <p>Networks without any encryption allow anyone to intercept and read your data.</p>
            </div>
            
            <div class="info-card">
                <h4>WPS Vulnerabilities</h4>
                <p>WiFi Protected Setup can have vulnerabilities that allow attackers to bypass security.</p>
            </div>
            
            <div class="info-card">
                <h4>Weak Passwords</h4>
                <p>Simple or common passwords can be cracked through dictionary attacks.</p>
            </div>
        </div>
    </div>
</div>
{{/layout}}
